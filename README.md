# osg-driver

Compute drivers for osg sandboxes (Docker-first; Podman/VM/K8s stubs).

Also owns bind-mount validation (`mounts`) and egress sidecar helpers (`sidecar`).

Module: `github.com/zorneth/osg-driver`
